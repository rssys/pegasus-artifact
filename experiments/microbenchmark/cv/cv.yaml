apiVersion: batch/v1
kind: Job
metadata:
  name: cv
spec:
  template:
    spec:
      terminationGracePeriodSeconds: 3
      restartPolicy: Never
      containers:
      - name: cv
        image: pdlan/pegasus-artifact-microbenchmark-cv
        command: ["/cv", "0"]
